##Python utility for sorting music files, primarily by artist and album, like most people do.

###The sorting stuff is all inside `sortMusicFiles.py` and the Song class that that file depends on is in `Song.py`
